# ── OBLIGATOIRE ───────────────────────────────────────────────────────────────
# Générez avec : openssl rand -hex 32
SECRET_KEY=remplacez-moi-avec-openssl-rand-hex-32

# ── CORS (tunnel SSH) ─────────────────────────────────────────────────────────
# Si vous accédez via tunnel SSH (ssh -L 8080:localhost:80 user@serveur),
# ajoutez le port local du tunnel :
CORS_ORIGINS=http://localhost,http://localhost:3000,http://localhost:8080

# ── Optionnel ─────────────────────────────────────────────────────────────────
# OLLAMA_AVAILABLE_MODELS=gemma3:4b,llama3.1:latest
# TOP_K=5
# DEBUG=false
