# ── OBLIGATOIRE ───────────────────────────────────────────────────────────────
# Générez avec : openssl rand -hex 32
SECRET_KEY=remplacez-moi-avec-openssl-rand-hex-32

# ── CORS ──────────────────────────────────────────────────────────────────────
# URLs séparées par des virgules, sans espaces, sans guillemets, sans crochets
# Ajouter ici le port de ton tunnel SSH si besoin (ex: http://localhost:8080)
CORS_ORIGINS=http://localhost,http://localhost:3000,http://localhost:8080

# ── Optionnel ─────────────────────────────────────────────────────────────────
# Modèles LLM disponibles dans l'interface (séparés par des virgules)
# OLLAMA_AVAILABLE_MODELS=gemma3:4b,llama3.1:latest

# Nombre de chunks retournés par la recherche Qdrant
# TOP_K=5

# Activer les logs détaillés
# DEBUG=false
