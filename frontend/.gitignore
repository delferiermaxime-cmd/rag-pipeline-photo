# Dépendances — réinstallées proprement dans le stage deps
node_modules
.npm

# Build Next.js local — on repart de zéro dans le builder
.next

# Fichiers sensibles — ne jamais envoyer dans le contexte Docker
.env
.env.local
.env.production

# Git et IDE
.git
.gitignore
.vscode
.idea

# Logs et tmp
*.log
npm-debug.log*
